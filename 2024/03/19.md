## 取り組んだ課題

### シングルページアプリケーション

- Rails と React で twitter クローンを作る
  - タスク 15
    - FE:通知画面
    - BE:通知 api の作成

## わかったこと

- モデル内で `attribute` を使用することでレコード作成時に指定した属性に可変のデフォルト値を設ける等ができる
- `before_create` 等のコールバック内でエラーが起きた際の挙動として、処理中のレコードはロールバックされる
  - これによってトランザクションのような挙動として扱うこともできる

## 次やること

### シングルページアプリケーション

- Rails と React で twitter クローンを作る
  - タスク 15
    - FE:通知画面のコメントに対するイベント設定

## 感じたこと

- rails 単体でのクローンよりも「こうやってみよう」が結構浮かんできて楽しい

## 学習時間

Today:4h Total:992h
